import 'package:flutter/material.dart';

class CellsPage extends StatelessWidget {
  const CellsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cells Page'),
      ),
    );
  }
}
